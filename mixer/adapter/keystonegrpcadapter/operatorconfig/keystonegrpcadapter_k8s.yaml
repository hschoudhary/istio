apiVersion: v1
kind: Service
metadata:
  name: keystonegrpcadapter
  namespace: istio-system
  labels:
    app: keystonegrpcadapter
spec:
  type: ClusterIP
  ports:
  - name: grpc
    protocol: TCP
    port: 9090
    targetPort: 9090
  selector:
    app: keystonegrpcadapter
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: keystonegrpcadapter
  namespace: istio-system
  labels:
    app: keystonegrpcadapter
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: keystonegrpcadapter
      annotations:
        sidecar.istio.io/inject: "false"
        scheduler.alpha.kubernetes.io/critical-pod: ""
    spec:
      containers:
      - name: keystonegrpcadapter
        image: istio/istio-keystone-adapter:1.0
        ports:
        - containerPort: 9090
